# Self-Healing JSON Validator — MCP Server dependencies

# MCP SDK — official Python implementation
mcp>=1.3.0,<2.0.0

# SSE web server
starlette>=0.40.0,<1.0.0
uvicorn>=0.32.0,<1.0.0

# Google Gemini SDK — LLM-assisted repair
google-genai>=1.0.0,<2.0.0

# JSON Schema validation (Draft-07 support)
jsonschema[format-nongpl]>=4.23.0,<5.0.0

# Pydantic — runtime data modelling
pydantic>=2.9.0,<3.0.0

# Stripe — metered billing
stripe>=10.0.0,<11.0.0

# HTTP client — Supabase REST calls
httpx>=0.27.0,<1.0.0
